import React from 'react';

export default () => (
  <div>
    <h3>{'Barkskins'}</h3>
    <div>{'A (mostly) text-based colony sim.'}</div>
    <div className="+push-bottom-quad">{'0.0.1'}</div>
  </div>
);
