import { createResolver } from '_utils/resolver';
import RNG from '_utils/chance';

const doResearch = (state) => {
  return state;
};

const researchResolver = createResolver([

]);

export default researchResolver;
